{
  "scripts": {
    "build": "react-scripts build",
    "serve": "node ./node_modules/serve/build/main.js -s build",
    "buildAndServe": "yarn build & yarn serve"
  },
  "dependencies": {
    "@grafana/faro-web-sdk": "^1.3.6",
    "@grafana/faro-web-tracing": "^1.3.6",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "^5.0.1"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "serve": "^14.2.1"
  }
}
